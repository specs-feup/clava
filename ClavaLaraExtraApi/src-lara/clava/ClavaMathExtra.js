class ClavaMathExtra {
	
	/**
	 * Attempts to simplify a C/C++ expression, returning an $expression join point with 
	 * the result of the simplification, or undefined if it was not possible to simplify
	 * the expression.
	 *
	 * @param {$expression} $expression - The C/C++ expression to simplify.
	 * @param {object} constants - An object that maps variable names to constants.
	 *
	 * @returns {$expression} the simplified expression, or undefined if it was not possible 
	 * to simplify the expression
	 *
	 */
	static simplify($expression, constants) {
		throw new Error("Not yet implemented");
	}
	
}