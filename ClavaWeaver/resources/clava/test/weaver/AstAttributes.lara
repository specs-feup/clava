aspectdef AstAttributes

	select vardecl{"a"} end
	apply
		println("Type kind:" + $vardecl.type.kind);
		println("Is builtin? " + $vardecl.type.getAstIsInstance("BuiltinType"));
		println("Is decl? " + $vardecl.getAstIsInstance("Decl"));
	end
	
end
